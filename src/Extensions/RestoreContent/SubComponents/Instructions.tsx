export function Instructions() {
  return (
    <p className="scrivito_notice_body">
      Restoring the initial content requires REST API write access. Using the
      REST API credentials in a browser can be a security risk.
      <br />
      Creating a new API key and deleting it afterwards is recommended.
    </p>
  )
}
